import RequirerInterface from './RequirerInterface';
/**
 * @inheritDoc
 */
export default class Requirer implements RequirerInterface {
    private nodeRequire;
    constructor(nodeRequire: Function);
    /**
     * @inheritDoc
     */
    require(path: string): RootConfig | SubConfig;
}
