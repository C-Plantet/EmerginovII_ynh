import LoaderInterface from './LoaderInterface';
import RequirerInterface from './RequirerInterface';
/**
 * @inheritDoc
 */
export default class Loader implements LoaderInterface {
    private existsSync;
    private requirer;
    private fileName;
    constructor(existsSync: (path: string) => boolean, requirer: RequirerInterface, fileName: string);
    /**
     * @inheritDoc
     */
    load(searchDirectories: string[]): RootConfig | SubConfig;
}
